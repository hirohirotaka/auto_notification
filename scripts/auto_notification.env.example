# Example environment file for auto_notification (copy to /etc/default/auto_notification)
# IMPORTANT: do not commit real secrets. Set values on the server and protect file permissions (600).

# SMTP / Mail settings (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your_app_password_here
FROM_EMAIL=your@gmail.com

# Optional providers (prefer APIs for reliability)
#SENDGRID_API_KEY=
#MAILGUN_API_KEY=
#MAILGUN_DOMAIN=

# Polling interval in seconds (default 60)
POLL_INTERVAL=60

# Paths (if you moved the project)
#WORKDIR=/var/lib/auto_notification
#RECIPIENTS_FILE=/var/lib/auto_notification/recipients.txt

# Logging can be left to the app (notification.log) or to systemd journal
